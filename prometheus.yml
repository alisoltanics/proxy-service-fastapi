global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'proxy-service'
    static_configs:
      - targets: ['app:8000']
    metrics_path: '/metrics/prometheus'
    authorization:
      credentials: 'proxy-metrics-secret'
